# TinyDB cross-screen mismatch sample

This minimal MIT App Inventor project deliberately stores a value under the
`profile_name` tag on Screen1 and reads `profile-name` on Screen2. Rebuild the
`.aia` fixture by zipping the contents of this directory, preserving the `src/`
and `youngandroidproject/` paths.
